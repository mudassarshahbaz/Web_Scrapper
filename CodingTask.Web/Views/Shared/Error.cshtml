@model ErrorViewModel
@{
    ViewData["Title"] = "Error";
}

<h1 class="text-danger">Oops! Something went wrong.</h1>
<h2 class="text-danger">We're sorry, but there was an error processing your request.</h2>

@if (Model.ShowRequestId)
{
    <p>
        <strong>Error details:</strong> <code>@Model.RequestId</code>
    </p>
}

<h3>What to do next</h3>
<p>
    If you need further assistance, please contact our support team at support@example.com.
</p>
<p>
    You can also try one of the following options:
</p>
<ul>
    <li>Go back to the <a href="/">homepage</a></li>
    <li>Search for what you're looking for using our <a href="/search">search page</a></li>
</ul>
<p>
    If you're a developer and need more information about the error, you can switch to the <strong>Development</strong> environment by setting the <code>ASPNETCORE_ENVIRONMENT</code> environment variable to <code>Development</code>.
</p>
<p>
    <strong>Note:</strong> The Development environment should only be enabled for local debugging, not for deployed applications.
</p>
